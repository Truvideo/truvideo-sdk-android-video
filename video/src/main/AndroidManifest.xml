<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application>

        <activity
            android:name=".ui.activities.edit.TruvideoSdkVideoEditActivity"
            android:exported="false"
            android:label="@string/title_activity_trimming_compose"
            android:theme="@style/Theme.TruVideoSDKVideo" />


        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="com.truvideo.sdk.video.TruvideoSdkVideoInitializer"
                android:value="androidx.startup" />
        </provider>

    </application>
</manifest>